---
title: "deploy"
description: "Deprecated compatibility alias for orchestration sync"
---

## strait deploy push

Deprecated. Use [`strait sync`](/cli-reference/sync) instead.

```bash
strait sync
```

During the transition, `strait deploy push` delegates to the same
orchestration sync behavior. It does not build images, push containers, select
regions, run canary deployments, or host customer code.
